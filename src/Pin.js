import { render } from '@testing-library/react';
import React from 'react';
import data from './data'

class Pin extends React.Component {
    render() {
        return <Infinity id={id} title={title} content={content} year={year}/>
    }
}

export default Pin;